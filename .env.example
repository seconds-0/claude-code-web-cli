# Database (Neon Postgres)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Clerk Auth
CLERK_SECRET_KEY=sk_test_xxxxx
CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
CLERK_ISSUER_URL=https://your-app.clerk.accounts.dev

# Upstash Redis (for job queue)
UPSTASH_REDIS_REST_URL=https://xxxxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=xxxxx

# Stripe (when needed)
STRIPE_SECRET_KEY=sk_test_xxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxx

# Hetzner Cloud (for provisioning)
HETZNER_API_TOKEN=xxxxx
HETZNER_PACKER_IMAGE_ID=123456789          # Set after building Packer image
HETZNER_LOCATION=nbg1                       # Nuremberg datacenter
HETZNER_SERVER_TYPE=cpx11                   # 2 vCPU, 2GB RAM

# Tailscale (for private networking)
TAILSCALE_API_KEY=tskey-api-xxxxx           # API key (not auth key)
TAILSCALE_TAILNET=your-org.github           # Your tailnet name

# Cloudflare R2 (for backups)
R2_ACCESS_KEY_ID=xxxxx
R2_SECRET_ACCESS_KEY=xxxxx
R2_BUCKET=claude-code-cloud-backups
R2_ENDPOINT=https://xxxxx.r2.cloudflarestorage.com

# Deepgram (for voice)
DEEPGRAM_API_KEY=xxxxx

# Frontend URLs
NEXT_PUBLIC_CONTROL_PLANE_URL=http://localhost:8080     # Control plane API URL
ALLOWED_ORIGINS=http://localhost:3000                    # CORS allowed origins

# Server
PORT=8080                                                # Control plane port

# Admin API (for orphan cleanup endpoints)
ADMIN_SECRET=                                            # Generate with: openssl rand -hex 32
